---
topic: File formats and parsing for querbeet (CSV, JSON, XLSX, Parquet)
type: technical
decision: Which browser-side libraries and strategies for reading and writing CSV, JSON, XLSX and possibly Parquet in a single-HTML no-backend app
preset: standard
updated: 2026-08-01T12:37
---

- (decision) Plan approved by Ben: technical run, breadth-first, 5 dimensions (D1 CSV/PapaParse, D2 encoding detection [select], D3 XLSX incl. SheetJS licence/distribution [select], D4 JSON repair + nested preview/flatten, D5 Parquet export feasibility gate). Preset standard (3 researchers, 8 sources/round, depth 2), validation normal, red_team off. NO HTML report — markdown only, per user. R2 (UI framework) runs in parallel elsewhere; this run does not touch it.
- (assumption) Hard gates inherited from idea.md section 3 (project context, not evidence): single HTML file, no server, CDN-loadable or inlinable, ~100k rows, Firefox 145+/Edge 143+/Chrome 143+ (Safari optional), licence must permit free unrestricted use, must survive German data reality (umlauts, 1.234,56, Windows-1252 Excel exports).
- (source) D2 r1 batch: WHATWG Encoding Standard (2026-05-21), MDN TextDecoder, Microsoft file-formats support page, npm registry API for jschardet/chardet/detect-character-encoding/encoding/iconv-lite, python-chardet issues #84/#185, johndcook.com BOM post. Digest: digests/encoding-r1-1.md
- (claim) ref=[D2-1] status=verified class=spec pub=2026-05 — WHATWG Encoding Standard makes windows-1252 a mandatory TextDecoder encoding and closes the set ('user agents must not support any other encodings'), so decoding CP1252 needs no library.
- (claim) ref=[D2-2] status=verified class=spec pub=2026-05 — iso-8859-1/latin1/us-ascii are all labels for the windows-1252 decoder in the Encoding Standard; the 1252-vs-8859-1 discrimination problem does not exist in a browser.
- (claim) ref=[D2-3] status=verified class=licence pub=2026-08 — jschardet is LGPL-2.1+ (npm registry, 3.1.4 published 2024-09-30, 4.0.0-rc.1 on 2026-07-27); chardet 2.2.0 (2026-06-20) is MIT with a browser field and zero deps.
- (claim) ref=[D2-4] status=unverified class=pattern pub=undated — Excel's plain 'CSV (comma delimited)' export writes Windows-1252/ANSI in German locales: Microsoft documentation is SILENT on code pages; only secondary consensus supports it. Design must not depend on a vendor promise.
- (question) D2 open: no measured accuracy figure for any charset detector on short Western-European text; min+gzip sizes for chardet/jschardet browser builds unknown; File.arrayBuffer/streaming TextDecoder pattern (Q7) not researched — carry to round 2.
- (source) D1 r1 batch: npm registry (papaparse, udsv, d3-dsv, csv-parse, web-csv-toolbox, csv42), GitHub releases atom + commits API for mholt/PapaParse, papaparse.com/docs, PapaParse source on master, uDSV benchmark repo, LeanyLabs benchmark. Digest: digests/csv-parsing-r1-1.md
- (source) D3 r1 batch: SheetJS docs (installation/standalone, csf/cell, csf/features/dates), sheetjs.com/pro, SheetJS Gitea API (tags + commits), npm registry API (xlsx, exceljs, write-excel-file, read-excel-file, xlsx-populate, @office-kit/xlsx, xlsx-kit), GitHub API exceljs, measured CDN artefacts. Digest: digests/xlsx-r1-1.md
- (event) Original measurement run by lead: PapaParse 5.5.4 from jsDelivr executed under Node v26.5.0 to verify the D1 derived claims. Script + output preserved in imports/verify-papaparse.mjs and imports/papaparse-5.5.4-measured-output.txt.
- (claim) ref=[D1-1] status=verified class=failure pub=2026-08 — MEASURED: PapaParse 5.5.4 dynamicTyping:true converts German '1.234' to the number 1.234 (1000x wrong, silent), '0123' to 123 (leading zeros lost), while '1.234,56' and '1.234.567' stay strings — producing a mixed-type column. Confirmed by execution, not derivation.
- (claim) ref=[D1-2] status=overturned class=pattern pub=2026-08 — The digest's derived claim that PapaParse's delimiter guesser fails on a preamble before the header is WRONG. Measured: a 3-line German preamble still yields ';' correctly; so do two-column semicolon files and semicolon files with comma decimals. Only a genuine single-column file falls back to ',', and it emits an explicit UndetectableDelimiter error rather than failing silently.
- (claim) ref=[D3-1] status=verified class=ecosystem pub=2026-08 — SheetJS CE is maintained-but-unreleased: newest tag v0.20.3 dated 2024-07-18 (~24 months), yet master commits continue into 2026-02. npm's xlsx package is stale at 0.18.5 (2022-03-24); cdn.sheetjs.com is the authoritative source. Licence Apache-2.0.
- (claim) ref=[D3-2] status=verified class=licence pub=2026-08 — SheetJS CE/Pro line: number formats (cell 'z') and column widths are CE; all cell styling (bold, fills, borders), conditional formatting and 'international locale support' are Pro, priced by quote only. Measured bundle: xlsx.full.min.js = 951,904 B raw / 334,805 B gzip; the smaller mini build drops CSV/SYLK encodings and is explicitly wrong for non-US users.
- (claim) ref=[D3-3] status=verified class=ecosystem pub=2026-08 — ExcelJS is dormant: stable 4.4.0 published 2023-10-19, last default-branch commit 2024-01-12, 798 open issues, not archived. write-excel-file 4.1.1 (2026-06-08) and read-excel-file 9.3.5 (2026-07-28) are the actively-released MIT alternatives; browser bundle viability unverified.
- (source) D4 r1 batch: npm registry API (jsonrepair, best-effort-json-parser, partial-json, json5, dirty-json, untruncate-json, @streamparser/json, stream-json, oboe, clarinet, json-formatter-js, vanilla-jsoneditor, jsoneditor, @textea/json-viewer, react-json-view, flat, csv42, @json2csv/plainjs, dot-object, flatten-json), GitHub API repo+commits for the live ones, Bundlephobia sizes, READMEs of jsonrepair/flat/csv42/json2csv, jsonrepair issue search, TC39 proposals. Digest: digests/json-r1-1.md
- (claim) ref=[D4-1] status=verified class=version pub=2026-07 — jsonrepair 3.15.0 (2026-07-03), ISC, ~2.88 KB gzipped, zero deps, ships a UMD build; documented repair list covers all LLM-characteristic breakage including truncation, markdown code fences, Python None/True/False, and NDJSON-to-array.
- (claim) ref=[D4-2] status=verified class=failure pub=2026-08 — The brief's premise that jsonrepair silently produces wrong data is UNSUPPORTED: an issue-tracker pass found only thrown errors (#170 stack overflow 2026-06-12, #159, #137), no live corruption reports. The one silent-failure issue (#102) was closed in 2023. Negative result from one search pass, not proof of absence.
- (claim) ref=[D4-3] status=verified class=pattern pub=2026-08 — Flattening semantics diverge across all three maintained tools: flat index-suffixes arrays (a.0, a.1), csv42 serialises each array into one JSON-string cell, json2csv makes explode an opt-in 'unwind' transform. Dot separator is the only shared convention. No default is right for both joinability and addressability — this is a UI decision, not a library default.
- (claim) ref=[D4-4] status=unverified class=performance pub=2026-08 — No measured browser numbers for JSON.parse on large files were found; only the V8 single-string cap (536,870,888 chars) from one aggregated retrieval. Two-source bar NOT met; do not rest a recommendation on it.
- (source) D5 r1 batch: npm registry (hyparquet, hyparquet-writer, hyparquet-compressors, hysnappy, parquet-wasm, apache-arrow, parquetjs, parquetjs-lite, @dsnp/parquetjs, @duckdb/duckdb-wasm), GitHub API for hyparam/* and kylebarron/parquet-wasm, jsDelivr data API file listings, parquet.apache.org implementation-status page, plus an ORIGINAL round-trip measurement. Digest: digests/parquet-r1-1.md
- (decision) D5 GATE PASSES — Parquet stays in scope. hyparquet-writer 0.16.3 (MIT, 2026-07-31) bundles to 57,703 B min / 17,239 B gzip as a self-contained IIFE with no WASM at all. This overturns the plan's expectation that Parquet would likely be dropped.
- (claim) ref=[D5-1] status=verified class=performance pub=2026-08 — MEASURED: hyparquet-writer 0.16.3 wrote 100,000 rows x 5 cols in 258 ms SNAPPY (2,321,579 B out, 54.4 MB heap delta) / 159 ms UNCOMPRESSED / 351 ms GZIP via fflate. Node on Linux x86-64, NOT browser-measured — same V8 and pure-JS path, but browser timing is unverified.
- (claim) ref=[D5-2] status=verified class=pattern pub=2026-08 — MEASURED interop: files written by hyparquet-writer 0.16.3 were read correctly by pyarrow 25.0.0 (exact schema, null count 33,334), DuckDB 1.5.5 (aggregates match) and Polars 1.43.2 (shape 100000x5, dtypes match), across SNAPPY, UNCOMPRESSED and GZIP. Corroborated: Apache Parquet's own implementation-status page lists hyparquet (page modified 2026-07-07).
- (claim) ref=[D5-3] status=verified class=performance pub=2026-08 — parquet-wasm 0.7.2 WASM payload measured at 6,494,208 B raw / 1,813,165 B gzip, rising to 8,658,944 B base64 (2,680,916 gzip) if inlined; default path fetches the .wasm at runtime, breaking a single-file build. DuckDB-WASM is 35.6-41.3 MB per variant. Both fail the single-file gate on size, not capability.
- (claim) ref=[D5-4] status=verified class=failure pub=2026-08 — apache-arrow JS 21.2.0 does NOT write Parquet at all (Arrow IPC only, zero 'parquet' occurrences in its type definitions). parquetjs (2019-10-03) and parquetjs-lite (2020-04-22) are dead; @dsnp/parquetjs 1.8.9 is alive but Node-shaped (7.1 MB unpacked, 13 deps incl. aws-sdk).
- (assumption) D5: hyparquet-writer publishes ESM only — no UMD/IIFE artefact exists. The 17,239 B gzip figure comes from bundling the published ESM with esbuild --bundle --minify --format=iife. A one-time bundle step is therefore required for the Parquet path, unlike PapaParse/jsonrepair which ship UMD.
- (source) D3 r2 batch: jsDelivr artefacts + data API for write-excel-file@4.1.1, read-excel-file@9.3.5, @office-kit/xlsx@0.9.0, xlsx-kit@0.8.0; both READMEs read from the CDN; npm registry; GitHub API for office-kit/xlsx (repo, contributors, commit count, full tree) and SheetJS GitHub mirror issues #1537/#1652 and exceljs #2299; SheetJS Gitea tags + commits; cdn.sheetjs.com/xlsx-latest artefact; docs.sheetjs.com csf/sheet, demos/bigdata/{,stream,worker}. Digest: digests/xlsx-r2-1.md
- (event) Original measurement by lead: write-excel-file 4.1.1 installed from npm and executed under Node v26.5.0, output verified with openpyxl 3.1.5. Artefacts in imports/verify-write-excel-file.{mjs,py} and imports/write-excel-file-measured-output.txt.
- (claim) ref=[D3-4] status=verified class=pattern pub=2026-08 — MEASURED: write-excel-file 4.1.1 output verified by openpyxl 3.1.5 — umlauts and the euro sign survive as UTF-8, leading zeros preserved as text ('0123'), numbers as real float/int carrying format codes #,##0.00 and #,##0, dates as real datetimes with dd.mm.yyyy, bold header applied, all five column widths applied, 100k-row file valid and fully readable (100,001 rows).
- (claim) ref=[D3-5] status=verified class=performance pub=2026-08 — MEASURED: write-excel-file 4.1.1 writes 100,000 rows x 5 cols in 3,343 ms producing 3,340,462 bytes (Node v26.5.0, Linux x86-64). That is ~13x slower than hyparquet-writer's 258 ms for comparable data and long enough to freeze a tab — the xlsx export path needs a Web Worker or a progress indicator.
- (claim) ref=[D3-6] status=verified class=failure pub=2026-08 — MEASURED TRAP: write-excel-file 4.1.1's Node entry returns a writer object {toBuffer,toStream,toFile}; the README's documented 'filePath' option is accepted and SILENTLY IGNORED — no file, no error. Also, the generated worksheet XML carries no <dimension> element, so openpyxl's read_only mode reports max_row=None.
- (claim) ref=[D3-1] status=verified class=ecosystem pub=2026-08 — REVISION of [D3-1]: SheetJS is quieter than round 1 suggested. No tag newer than v0.20.3 (2024-07-18) exists and the cdn.sheetjs.com/xlsx-latest alias still resolves to 0.20.3 (version string grepped from the artefact). Master's newest commit is 2026-02-09, i.e. ~6 months of silence, not ongoing activity. Docs were still edited 2026-03-23. No vendor statement explaining the gap could be found; the Gitea issue tracker and releases API refuse anonymous access.
- (claim) ref=[D3-7] status=verified class=version pub=2026-08 — write-excel-file and read-excel-file both ship real UMD bundles exposing globals writeXlsxFile/readXlsxFile with zero require( or process. references. Measured: 70,844 B raw / 19,132 B gzip and 52,613 B / 15,254 B — combined 34,386 B gzip against SheetJS's ~334,000 B, roughly a 10x reduction, with cell styling and conditional formatting included rather than paywalled.
- (claim) ref=[D3-8] status=verified class=ecosystem pub=2026-08 — @office-kit/xlsx and xlsx-kit are ONE renamed codebase, not two candidates (identical 220-file jsDelivr listings, byte-identical type definitions, old repo 404s). It is real engineering (662 commits, 342 test files) but effectively single-author, ships ESM-only with no root export (jsDelivr /+esm returns 'Couldn't find the requested file.'), declares a Node 22+ floor, and its own README routes browser readers to read-excel-file.
- (question) D3 OPEN RISK: read-excel-file 9.x spawns a Web Worker internally (dependency 'worker-f'). Unverified whether that works from a file:// single HTML page. This is the SAME open question R1 left for R4. If it fails, fallbacks are the read-excel-file/web-worker export path or 8.x.
- (event) research.md assembled: executive summary, D1-D5, cross-dimension insights, 8 recommendations, 6 open questions, 44-source appendix + 3 original measurements, staleness map. Citation check mechanical pass: ok, no dangling markers, no orphaned rows. research-plan.md updated: R3 marked done, findings fed forward into R4 (worker question sharpened) and R5 (German parser is load-bearing, encoding settled, ESM/build-step cost).
- (event) Citation-verification pass (fresh context) complete. 1 claim CONTRADICTED (read-excel-file internal worker — README retracts its own statement; rewritten), 6 numbers DRIFTED (xlsx timing 3343 not in artefact; Parquet timings were the superseded run; GZIP timing not like-for-like; Polars never read the uncompressed file; jsonrepair #102 closed 2024-02-09 not Oct 2023; PapaParse issues 224 not 190), 1 internal contradiction resolved (csv42 declares NO licence on npm at any level). All D2 WHATWG claims and all byte sizes CONFIRMED. Corrections applied; a Verification note section records them.
- (claim) ref=[D3-9] status=overturned class=failure pub=2026-08 — OVERTURNS the earlier reading that read-excel-file 9.x unconditionally spawns an internal Web Worker. Its README contains BOTH the claim (v9 migration notes, live) and an HTML-comment retraction labelled 'Previous inaccurate statement', plus live prose saying the XML parser blocks the main thread. worker-f is a real dependency, so something constructs a worker — most plausibly the unzip step. Source-reading required; the README cannot settle it.
- (claim) ref=[D3-10] status=verified class=pattern pub=2026-08 — R2 (sibling run) measured the file:// boundary in Chromium 150 and Firefox 153: a CLASSIC Worker from a blob: URL works in both; a module Worker from a blob URL works only in Firefox; new Worker('./sibling.js') fails in both. This closes R1's open worker question and makes the xlsx-export worker buildable. Measured in R2, not here.
- (claim) ref=[D4-5] status=verified class=licence pub=2026-08 — csv42 declares NO licence field on npm, neither top-level nor per-version (checked directly against registry.npmjs.org/csv42, 5.0.3). Two researchers on this run disagreed; the registry settles it.
- (event) session complete
