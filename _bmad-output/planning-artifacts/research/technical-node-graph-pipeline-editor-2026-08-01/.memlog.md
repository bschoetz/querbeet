---
topic: Node-graph pipeline editor
type: technical
decision: What carries a node-graph editor for ~5-30 nodes under querbeet's single-file file:// constraints
preset: standard
updated: 2026-08-01T16:17
---

- (decision) Plan gate approved: technical/select, 4 dimensions (D1 candidate screen, D2 evidence per criterion, D3 single-file build+file:// measurement, D4 dropped). Hard gates G1 single-file/no runtime fetch, G2 Vue 3 first-class, G3 release <=12mo, G4 permissive licence verified in published npm package, G5 graph never owns/watches the tables. Footprint explicitly not a criterion. Preset standard, validation normal, no HTML briefing.
- (decision) PROJECT DECISION 2026-08-01 (Ben, at the plan gate): keyboard navigability is NOT a selection criterion for R6 - neither hard gate nor weighted. This overrides PRD FR-12's clause 'The graph is navigable and editable by keyboard' and NFR-7 for the purpose of this selection. Recorded so FR-12 is not later assumed satisfied.
- (decision) Build scope D3 (Ben): top-2 screening winners built as real single-file Vite artifacts PLUS a minimal hand-built SVG canvas as baseline, so the hand-built cost claim is measured rather than estimated.
- (source) Lead-measured primary batch: npm registry JSON API (registry.npmjs.org) for 18 packages + published tarballs installed and inspected on disk (npm 12.0.1 / Node 26.5.0). Written to imports/npm-package-audit-2026-08-01.md and imports/npm-registry-2026-08-01.json.
- (claim) ref=[M1] status=verified class=licence pub=2026-08 — None of the four Rete packages (rete 2.0.6, rete-vue-plugin 2.1.3, rete-area-plugin 2.3.2, rete-connection-plugin 2.0.5) ships a LICENSE file in the published tarball; the MIT claim rests only on the package.json license field. Vue Flow and all @baklavajs packages do ship one.
- (claim) ref=[M2] status=verified class=architecture pub=2026-08 — No shipped .js/.mjs/.cjs/.css file of @vue-flow/core, @baklavajs/* or the rete family contains import(, fetch(, new Worker, importScripts, XMLHttpRequest or @font-face; no CSS file contains any url(). All three families pass the no-runtime-fetch gate at package level.
- (claim) ref=[M3] status=verified class=version pub=2026-08 — rete-vue-plugin 2.1.3 declares peer vue '^2.6 || ^3.2' and resolves vue-demi transitively: it is a Vue2/Vue3-straddling renderer, not Vue-3-native.
- (claim) ref=[M4] status=verified class=ecosystem pub=2026-08 — Gate G3 (release since 2025-08-01) fails outright for drawflow 0.0.60 (2024-09-03), litegraph.js 0.7.18 (2024-01-08) and @jsplumb/browser-ui 6.2.10 (2023-07-14). gojs 4.0.3 fails G4: manifest license is 'SEE LICENSE IN license.html'. jointjs was renamed to @joint/core 4.3.1 (2026-07-27, MPL-2.0, 15 releases in window) - screening the old name alone would misjudge it as dead.
- (source) Lead-measured D3 build probe: three single-file Vite artifacts (vue-flow 1.48.2, baklavajs 2.8.1, hand-built SVG) opened from file:// in Chromium 151.0.7922.34 and Firefox 153.0 via Playwright. Sources + raw JSON in imports/graph-probe/, note in imports/graph-probe-measurement-2026-08-01.md
- (claim) ref=[M5] status=verified class=architecture pub=2026-08 — All three candidates build to exactly one HTML file (224,256 / 175,107 / 73,406 B), contain zero import(/fetch(/new Worker/importScripts/@font-face/non-data url(), and issue ZERO network requests beyond the document when opened from a real file:// URL in Chromium 151 and Firefox 153, with no page errors. Gate G1 passed by all three.
- (claim) ref=[M6] status=verified class=architecture pub=2026-08 — G5 RESOLVED: no candidate proxies or mutates a frozen 100k x 20 table placed in node data. readback===frozenTable true, isReactive false, still frozen, 100,000 rows, in both engines. Vue's reactivity skips non-extensible objects, so Object.freeze is itself the protection and markRaw is unnecessary. This overturns the Vue-3 screen's reading of Vue Flow's markRaw docs as a G5 failure.
- (claim) ref=[M7] status=verified class=architecture pub=2026-08 — Vue Flow COPIES the node objects handed to it: findNode('s1')===nodes.value[0] is false, while data.table identity is preserved. The wrapper is copied, the payload passes through.
- (claim) ref=[M8] status=verified class=architecture pub=2026-08 — Cycle refusal (PRD FR-12): Vue Flow accepts a cycle via addEdges (3->4 edges, no error), shipping no cycle detection at all. BaklavaJS refuses a direct self-loop (connectionAllowed:false) but ALLOWS a two-node cycle, while containsCycle(graph) from @baklavajs/engine returns true immediately after - the detector exists but is not wired to the guard. Hand-built refuses with a named reason in 12 lines.
- (claim) ref=[M9] status=verified class=performance pub=2026-08 — Editor heap cost, Chromium: Vue Flow 2,762,588 B, BaklavaJS 1,708,404 B, hand-built 323,668 B, against ~94.3 MB for the frozen 100k x 20 table in all three. The editor costs 0.3-2.9 percent of one source table; footprint cannot decide this question.
- (claim) ref=[M10] status=verified class=ecosystem pub=2026-08 — BaklavaJS ships undo/redo (UNDO_COMMAND, REDO_COMMAND, useHistory, command handler), clipboard, subgraphs, sidebar and toolbar from @baklavajs/renderer-vue. Vue Flow ships none of these; React Flow's two highest-comment issues ever were undo/redo and auto-layout, both closed as do-it-yourself.
- (claim) ref=[M11] status=verified class=experience pub=2026-08 — A hand-built SVG canvas doing node drag, pan, cursor-anchored wheel zoom with screen<->graph conversion, connection dragging, cycle refusal with a named reason, orphan-step marking and connection hit-testing is 164 lines (graph.js 40 + Canvas.vue 124), excluding node components. No auto-layout, undo/redo, minimap, keyboard handling or multi-select.
- (claim) ref=[1] status=verified class=licence pub=2026-08 — Vue Flow is MIT with no paid tier, no licence key and no eligibility gate: vueflow.dev states 'Released under the MIT License' and only suggests optionally supporting Webkid via React Flow Pro. Closes the Vue-3 screen's absence-of-evidence flag.
- (claim) ref=[2] status=verified class=ecosystem pub=2026-08 — Vue Flow's last functional core commit is 2026-01-28; everything since is docs (2026-05-14 docs fix, 2026-06-23 docs). BaklavaJS's last functional commits are 2026-04-09 to 2026-04-11 and include a feature (forward engine) plus bug fixes. The smaller-starred project is the more actively developed one right now.
- (claim) ref=[3] status=verified class=architecture pub=2026-08 — PRD Open Question 4 (does R2's Vue 3 verdict survive a graph editor?) answered yes: all three probes are Vue 3 SFCs, per-kind <component :is> dispatch worked in every candidate, and mount ranged 18-97 ms across both engines with no errors.
- (event) Fresh-context citation verifier found the cost table quoted an overwritten probe run: the measurement note came from run 1, graph-probe-results.json from run 3. Probe re-run once more as a single clean pass; cost table, percentages, built sizes and mount range re-derived from that run. Also corrected: two [M*] citations that the measurements did not support, both matrix sensitivity thresholds, the built-size spread (49 KB not 150 KB), 'years of bugs', the frozen-rows scope, Vue Flow's last functional commit date, and a provenance claim citing the wrong URL.
- (claim) ref=[M9] status=verified class=performance pub=2026-08 — CORRECTED from a clean single run: editor heap cost Chromium 2,763,340 B (Vue Flow) / 1,125,656 B (Baklava) / 323,848 B (hand-built) against ~94.4 MB for the frozen table, i.e. 2.93 / 1.19 / 0.34 percent. Built sizes 224,382 / 175,233 / 73,532 B. Mount 11.9-68.1 ms across all six runs. Heap deltas vary run to run by up to ~0.6 MB; the ratio survives, the digits do not.
- (claim) ref=[M12] status=verified class=architecture pub=2026-08 — vue-flow-core.mjs 1.48.2 contains 0 occurrences of cycle, Cycle, acyclic and topological; 37 of isValidConnection and 4 of connectionExists, neither traced. This replaces the earlier claim that rested on a hand-written probe comment.
- (claim) ref=[M10] status=verified class=ecosystem pub=2026-08 — @baklavajs/renderer-vue 2.8.1 exports all 14 undo/redo, clipboard, subgraph, sidebar and toolbar symbols out of 75 exports; @vue-flow/core 1.48.2 has 69 exports, none matching hist|undo|redo|clipboard|subgraph|cycle. Artefact: imports/graph-probe/package-export-inspection.json
- (event) Run complete. All D3 figures re-verified mechanically against imports/graph-probe/graph-probe-results.json: 0 mismatches across built sizes, table heap, mount heap, percentages and mount timings for all three candidates in both engines. Citation check clean (13 markers, 13 appendix rows). Not run: the configured doc_standards polish (skill:bmad-review lenses=structure,prose).
- (event) doc_standards applied: skill:bmad-review lenses=structure,prose (Strategic/Context pyramid model). Accepted: 1 accuracy fix (recommendation 1 claimed cost measured in both browsers; it is Chromium-only), 11 prose fixes, 2 MOVEs (The pick above the matrix; [M11] appendix row), 3 CUT/MERGE of true duplication, 2 orienting lines under the empty H2s, terminology and spelling consistency. Declined with reason: condensing exec-summary finding 2 (the reviewer itself advises keeping the mechanism where the downstream skill reads it), condensing the verification note (trust-building detail), moving Graph semantics to the front of D3 (costs the section its arc), condensing The strongest argument against the pick.
- (decision) PROJECT DECISION 2026-08-01 (Ben): Vue Flow 1.48.2 over the hand-built canvas, overriding the research recommendation (85 vs 75). Reasoning carried over from R1/Arquero: a complete, widely used library is more battle-tested than freshly written bespoke code. Vue Flow over BaklavaJS on adoption (6760/300/11 vs 2045/228/2); R1's precedent makes dormancy explicitly not a blocker. Consequences recorded in the report section 'Adopting Vue Flow - what the measurements require': the cycle check must sit in front of addEdges (M12/M8), and the source-of-truth split must be decided because Vue Flow copies node wrappers (M7). Research verdict left standing as research.
