---
topic: querbeet — a single-file browser tool that consolidates report exports into one table via a portable, machine-authorable Recipe
updated: 2026-08-02T10:23
---

- (event) Create operation. No prior spec folder; slug 'querbeet' inherited from prd-querbeet-2026-08-01 and architecture-querbeet-2026-08-02.
- (direction) Ben chose: distill PRD + addendum as absorbed sources, adopt ARCHITECTURE-SPINE.md as a companion rather than absorbing it — the What/How separation upstream is already clean.
- (direction) Ben chose to leave project-context.md ungenerated; the spine's AD-1..AD-30 and Consistency Conventions already carry that ground, and a second copy would rot. persistent_facts therefore resolved to nothing this run.
- (decision) CAP-N is minted equal to FR-N from the PRD, so the spine's binds: [FR-1..FR-39] resolves against spec ids with no mapping table. New capabilities without an FR start at CAP-40.
- (decision) NFR-1..NFR-9 render as Constraints, not Capabilities: each rules an implementation out rather than naming something a user can do.
- (decision) SM-C1 and SM-C2 render as Constraints — 'do not optimise X' bends design decisions — while SM-1..SM-4 stay in the Success signal.
- (decision) Six spec-authored companions: glossary, users-and-journeys, acceptance-criteria, technology-decisions, decision-record, measured-constraints. Plus ARCHITECTURE-SPINE.md adopted.
- (decision) measured-constraints.md carries only findings the spine did not absorb into an AD; where the spine speaks it is authoritative and is not restated, to keep one place per rule.
- (decision) Research runs and spikes are cited by path from technology-decisions.md but not adopted as companions — apparatus, not contract.
- (decision) Addendum section 5's plain-arrays boundary contract is superseded by spine AD-5 (Table handle); the addendum already records the supersession, so no conflict needed resolving with Ben.
- (capability) CAP-1 (=FR-1) Load CSV/JSON/NDJSON/XLSX/Parquet files as named, individually removable Sources.
- (capability) CAP-2 (=FR-2) Determine character encoding with a visible per-Source override; BOM, then strict UTF-8 probe, then Windows-1252.
- (capability) CAP-3 (=FR-3) Propose CSV delimiter and header row, both correctable, both stored in the Recipe.
- (capability) CAP-4 (=FR-4) Detect that a JSON Source is malformed before anything else is attempted.
- (capability) CAP-5 (=FR-5) Offer repair of a malformed JSON Source; handled cases target what an LLM answer pasted into a file looks like.
- (capability) CAP-6 (=FR-6) Disclose what a repair changed, and carry the fact into the run status.
- (capability) CAP-7 (=FR-7) Flatten nested JSON with an explicit, changeable array strategy — three options, no hidden default.
- (capability) CAP-8 (=FR-8) Preview every Source; JSON and NDJSON additionally get a collapsible structure view.
- (capability) CAP-9 (=FR-9) Detect column types and locales over every value, report irreducible ambiguity as such, and block execution until confirmed.
- (capability) CAP-10 (=FR-10) Annotate columns in free text; annotations live in the Recipe and in the Column Profile.
- (capability) CAP-11 (=FR-11) Enter the Editor deliberately; loading a Recipe does not open it.
- (capability) CAP-12 (=FR-12) Compose a Pipeline as a DAG of Steps with one designated Result Step; cycles refused, broken and orphan Steps marked.
- (capability) CAP-13 (=FR-13) Union two or more tables with column mapping; columns present in only some inputs are listed before the Step runs.
- (capability) CAP-14 (=FR-14) Join two tables on one or more keys, reporting unmatched left rows, row-count growth, and optionally a duplicate audit.
- (capability) CAP-15 (=FR-15) Filter rows; comparison values held in canonical machine form and refused on type disagreement.
- (capability) CAP-16 (=FR-16) Select, rename and reorder columns; a duplicate name is refused.
- (capability) CAP-17 (=FR-17) Add a computed column from a fixed operation list — no formula language.
- (capability) CAP-18 (=FR-18) Aggregate by one or more grouping columns; null groups stay visible.
- (capability) CAP-19 (=FR-19) Preview every Step with its own warnings attached.
- (capability) CAP-38 (=FR-38) Execute the Pipeline under one owner across a live and an explicit mode, with the mode in force always stated.
- (capability) CAP-20 (=FR-20) Save and load a Recipe — versioned, data-free, documented well enough for a model to author one.
- (capability) CAP-21 (=FR-21) Derive and carry an Input Contract covering only the columns the Pipeline reads.
- (capability) CAP-22 (=FR-22) Run a Pre-flight Check before executing; fits, missing or doubtful per requirement.
- (capability) CAP-23 (=FR-23) Map the Consumer's actual columns onto unmet contract requirements, savable back into the Recipe.
- (capability) CAP-24 (=FR-24) Export and import a Package — a compressed Recipe plus its Source data, visibly distinct from a Recipe.
- (capability) CAP-25 (=FR-25) Persist the session and delete it in one obvious action; a partial restore says so.
- (capability) CAP-26 (=FR-26) Produce a Column Profile — structure, no cell values — shown in full before use.
- (capability) CAP-27 (=FR-27) Generate a copy-ready prompt block carrying the question, the profile, the current Pipeline and both format specs.
- (capability) CAP-28 (=FR-28) Accept a model's Recipe through one validator that names the failing reference specifically enough to paste back.
- (capability) CAP-29 (=FR-29) Execute a Probe Query locally and show its result before any return step.
- (capability) CAP-30 (=FR-30) Release sample values explicitly, per column per exchange, never persisted into the Recipe.
- (capability) CAP-31 (=FR-31) Display the Result as a table responsive at the design scale, in German display conventions.
- (capability) CAP-32 (=FR-32) Filter and sort the view transiently, and promote a view filter into a Filter Step in one action.
- (capability) CAP-33 (=FR-33) Search the full Result, not the rendered window.
- (capability) CAP-34 (=FR-34) Show a run status that aggregates every diagnostic of the run and travels into the exported document.
- (capability) CAP-35 (=FR-35) Compose a Dashboard from Tiles on a fixed grid, stored in the Recipe.
- (capability) CAP-36 (=FR-36) Export the Result as CSV, JSON, XLSX or Parquet in German conventions, off the main thread.
- (capability) CAP-37 (=FR-37) Export a self-contained static view document — HTML and PDF — with vector charts and selectable label text.
- (capability) CAP-39 (=FR-39) Detect structurally broken CSV and report it rather than parsing it into a plausible table; no repair attempted.
- (constraint) NFR-1 Delivery: one HTML file opened by double-click. A build step may produce it; using it must never require one.
- (constraint) NFR-2 Network silence: no network request of any kind, unconditional in the MVP, verifiable by grepping the built artifact.
- (constraint) NFR-3 Scale: ~100k rows per Source, ~500k total; responsive there, nothing beyond promised or obstructed.
- (constraint) NFR-4 Browsers: Chromium lead (Edge/Chrome 143+); Firefox 145+ measured not assumed and dropped rather than accommodated; Safari optional.
- (constraint) NFR-5 Form factor: desktop, Full HD. Mobile and tablet not attempted.
- (constraint) NFR-6 Language: interface German, code and documents English.
- (constraint) NFR-7 No WCAG level targeted, but no interaction may exist only as a pointer gesture; a drag computes a target and updates the model.
- (constraint) NFR-8 Data residence: values leave the browser only by user-triggered export or a disclosure the user saw. Qualified by R9 — file:// storage is one shared bucket and the UI must not imply isolation.
- (constraint) NFR-9 Nothing is typed in the MVP — no formula, expression, query or script. An MVP boundary, and the clicked path must stay complete whenever typing is added.
- (constraint) The characteristic failure mode is a plausible wrong number, not an error message. Every silent library behaviour measured in research must be made visible at the Step that caused it.
- (constraint) SM-C1 Do not optimise time-to-first-result; the CAP-9 gate and the CAP-22 check deliberately cost time.
- (constraint) SM-C2 Do not optimise breadth of Step kinds or graph expressiveness; every addition enlarges what a model must get right.
- (constraint) The Recipe format must be machine-authorable from documentation alone, and a linear pipeline must be the trivial case of the graph format.
- (constraint) ARCHITECTURE-SPINE.md is binding, not advisory: AD-1..AD-30 rule out implementations that would otherwise satisfy a capability.
- (assumption) UJ-2, the entire Consumer journey, is a hypothesis — Christian has not been interviewed.
- (assumption) The Boxchecker's needs are fully served by a self-describing export (CAP-37); no Boxchecker was consulted.
- (assumption) Exactly one Result Step is sufficient; a graph naturally admits several terminal nodes and this was not requested either way.
- (assumption) Locale ambiguity can be surfaced usefully rather than merely detected — a user can resolve what a parser cannot.
- (assumption) SM-2, a Recipe used by a second person, is the right proof of the core hypothesis; a single instance is weak evidence.
- (assumption) A static view document is sufficient for its decision-maker; the interactive variant was cut on cost, not on judgement.
- (assumption) The transformation path is separable enough to ship first, which makes the natural build order the risk order. Stated as an observation, never validated against an architecture.
- (question) How does the Consumer actually work today? Gates the shape of his first five minutes — landing state, how much Pre-flight Check a non-builder interprets, whether repair needs to be gentler than the Editor. Not reachable near-term.
- (question) Do write-excel-file's format codes render as German decimal commas in a real German Excel? Owner scheduled it for 2026-08-02; until it passes, 'opens cleanly in Excel' is asserted, not verified.
- (question) What container does a Package use, and does compressing 500k rows in the browser stay responsive? R9 answered only at its IndexedDB gate.
- (question) Does the Author need a way to test a Recipe against someone else's file shape without their data? Implied by the handoff, never discussed.
- (question) How is the view document of CAP-37 produced — library or browser print-to-PDF behind a print stylesheet? R8 written and unrun; the last MVP capability with no research behind it.
- (question) Which Vite major is the single-file plugin pinned against? Vite 8 replaced its bundler and the plugin has open upstream issues. Resolve by building.
- (question) How many rounds does the CAP-28 paste-the-error-back correction loop take? Five of five authoring runs passed first time, so the failure path has never been exercised.
- (question) How does a user connect two Steps by keyboard? Nine of eleven Editor interactions are keyboard-reachable; connecting is the gap and waits on a UX decision, not on anything technical.
- (event) Self-validate pass 1 (coherence) PASSED. All 39 capabilities carry both intent and success; ids 1..39 unique with none reused; 14 constraints each rule something out; 17 non-goals across permanent and MVP-boundary; success signal is two demonstrable events. No fix needed.
- (event) Self-validate pass 2 (preservation) PASSED after one fix. Every FR-1..FR-39 lands as CAP-N with a matching acceptance-criteria section; NFR-1..NFR-9 land as C-1..C-9; PRD sections 1,2,3,5,6,7,8,9 and addendum sections 1-7 all routed. Fix applied: BaklavaJS and @maxgraph/core had been de-named in measured-constraints.md, which cost a downstream reader the actionable fallback; both restored.
- (event) Wrapper-only content dropped, on the record: PRD section 0 (the PRD's self-description of its own numbering and audience), the inline [ASSUMPTION]/[NOTE FOR PM] tagging convention itself (its content was routed), the addendum's opening framing sentence, and the addendum's research-currency note (superseded by the spine's 2026-08-02 registry re-verification, which is cited in technology-decisions.md).
- (event) Deliberate non-restatements, not drops: the bar-width and long-label tile settings, the persist() 180 s deadlock, worker form, structured-clone costs, the chart renderer rule and the spacer guard all live in spine AD-9/AD-15/AD-16/AD-24/AD-26 and are cited rather than copied, per the one-rule-one-place decision.
- (event) Spec derived and written: SPEC.md plus six spec-authored companions, ~17,100 words total. Story Breakdown not offered this run.
- (event) Update run 2026-08-02, following a build-scaffold session and a spine amendment. Trigger: one open question resolved by building.
- (decision) Open question 'Which Vite major is the single-file plugin pinned against?' is CLOSED. Vite 8.2.0 with vite-plugin-singlefile 2.3.3 emits exactly one HTML file from the project's real config and the one-file assertion passes. Resolved the way the question specified — by building, not by reading.
- (constraint) One build-config consequence came with it and lives in spine AD-17, not here: build.modulePreload must be false, because Vite injects a polyfill that calls fetch(el.href). Dead code in a single-file bundle, but real fetch( in the artefact. Cited, not copied — the spine is the one place for it.
- (note) Not folded into the spec: the scaffold itself, the test envelope, and the per-scheme Firefox measurement. The spec is a what-to-build contract, not a status report, and the measurement is spine AD-18's.
- (event) Self-validate pass 1 (coherence) PASSED. 39 capabilities intact with intent and success, ids 1..39 unique and unrenumbered, 14 constraints, 7 open questions (was 8). No capability, constraint or non-goal touched by this update.
- (event) Self-validate pass 2 (preservation) PASSED. The closed question is recorded rather than deleted: technology-decisions.md states it was settled by building on 2026-08-02 and names what settled it. No orphaned reference to the Vite major remains. modulePreload verified absent from the spec — it lives in spine AD-17 and is cited, per the one-rule-one-place decision.
- (decision) CAP-36's Excel format-code strategy is DECIDED by spike xlsx-german-format-2026-08-02: write locale-neutral codes (#,##0.00, dd.mm.yyyy) and never a German-looking one. Verified end to end — the library passes codes through to styles.xml verbatim, cell types round-trip as real numbers and dates, and LibreOffice under de_DE renders all 13 cases correctly while the German-looking code #.##0,00 renders as 1.234,56000 and is a defect.
- (decision) Do not use the [$-407] locale prefix. It is unnecessary and it pins rendering to German rather than following the reader's locale, which is wrong for a file the recipient may open anywhere.
- (question) Narrowed, not closed: does Microsoft Excel agree with LibreOffice? Same specification and strong evidence, but a different product. The two plausible divergence points are the escaped currency literal and the locale prefix, neither of which querbeet uses. Now a one-open-one-look check against german-format-probe.xlsx rather than an investigation.
- (constraint) Two adapter-facing API facts, from the spike: write-excel-file 4.1.1 has no filePath option and returns {toBuffer,toStream,toFile}; read-excel-file 9.3.5 returns [{sheet,data}] rather than a flat grid when no sheet is named.
- (event) Self-validate pass 1 (coherence) PASSED. 39 capabilities intact, 14 constraints, 7 open questions — the Excel question narrowed rather than closed, so the count is unchanged and the wording says what shrank.
- (event) Self-validate pass 2 (preservation) PASSED. The spike's decided half landed in measured-constraints.md as a rule the adapter must follow; its undecided half stayed an open question naming what specifically is unconfirmed. Nothing from the source was dropped and nothing was overstated as verified.
- (event) Open question CLOSED 2026-08-02: Ben opened german-format-probe.xlsx in a real German Microsoft Excel and confirmed it renders correctly. CAP-36's 'opens cleanly in Excel' moves from asserted to verified. Evidence grade recorded in the spike: an owner's visual check against a sheet built to make the answer obvious, not an itemized per-row report.
- (event) Self-validate both passes PASSED. 39 capabilities intact and unrenumbered, 14 constraints, open questions 7 to 6. The closed question is recorded as closed with its evidence in measured-constraints.md and the spike's findings.md rather than deleted.
